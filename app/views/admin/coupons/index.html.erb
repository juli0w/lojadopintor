<%= heading "Cupons" %>

<table class="striped responsive-table">
  <thead>
    <tr>
        <th></th>
        <th>Cupom</th>
        <th>Ativo?</th>
        <th></th>
    </tr>
  </thead>

  <tbody>
    <% @coupons.each do |coupon| %>
      <tr>
        <td>#<%= coupon.id %></td>
        <td><%= coupon.name %></td>
        <td><%= coupon.active? ? "Sim" : "Não" %></td>
        <td><%= link_to "Editar", [:edit, :admin, coupon] %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<div class="row">
  <div class="col s12 admin-actions">
    <%= link_to "Cadastrar", new_admin_coupon_path, class: "btn" %>
  </div>
</div>
